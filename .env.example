# Copy this to .env and fill in your real token.
# Never commit .env itself.

HF_TOKEN=hf_your_token_here
# WSL2 laptop GPUs lack CUDA UVA support; MRV2 (vLLM default) requires it. Force legacy runner.
VLLM_USE_V2_MODEL_RUNNER=0
